server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hospital?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456789abc
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.hospital.monitor: debug

knife4j:
  enable: true
  setting:
    language: zh_cn
  openapi:
    title: 患者监控系统API
    description: 患者监控系统接口文档
    version: 1.0.0
    contact:
      name: hospital-dev
